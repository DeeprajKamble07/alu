# alu

This project implements a simple 8-bit Arithmetic Logic Unit (ALU) using Verilog HDL. The ALU performs a set of basic arithmetic and logical operations based on a 3-bit control signal. A complete testbench is included to simulate the design and observe waveform outputs.
